# Copy to .env.local and fill in. These are the ONLY variables the app needs.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx
VITE_AUTH_EMAIL_DOMAIN=olodnotary.so

# Do not put the service role key in this file or any VITE_ variable.
# It bypasses Row Level Security and Vite would inline it into the bundle.
